#include <stdio.h>

int main(void)
{
    // char toAscii;
    // printf("Enter a character: ");
    // scanf("%c", &toAscii);

    // printf("%i\n", toAscii);

    // int toChar;
    // printf("Enter a number from 0-127: ");
    // scanf("%i", &toChar);

    // printf("%c\n", (char)toChar);

    // ASCII Math

    char useMath = 'A' + '\t'; // \t -> tab

    printf("A(65) + \\t(11) = %c(%d)\n", useMath, useMath);

    return 0;
}