#include <stdio.h>

int main(void)
{

    _Bool isTrue = 144554; // this number is just arbitrary, as long as it is not 0, it is true
    _Bool isFalse = 0;

    printf("isTrue: %i, isFalse: %i\n", isTrue, isFalse); // 1, 0
    return 0;
}